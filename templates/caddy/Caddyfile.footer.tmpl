	# All subdomains
	@subdomains host *.{{DOMAIN}}
	handle @subdomains {
		respond "FayaaSRV route is configured for {http.request.host}" 200
	}

	# Fallback
	handle {
		respond "FayaaSRV Caddy is running" 200
	}
}
